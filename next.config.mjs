/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

module.exports = {
    output: 'export',
    basePath: '/myytatua', // substitua pelo nome do repositório
    assetPrefix: '/myytatua/', // substitua pelo nome do repositório
  };
  